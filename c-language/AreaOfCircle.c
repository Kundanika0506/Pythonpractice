#include <stdio.h>
int main(){
    // int r = 2;
    float pi = 3.14;
    // printf("the area of circle is %f", pi*r*r);

    int r ;
    scanf("%d", &r);
    printf("the area of the circle is %f", pi*r*r);
}
