#include <stdio.h>
int main(){
    float pi = 3.14;
    // int r = 2;
    // int h = 6;
    // printf("the volume of cylinder is %f", pi*r*r*h);

    int r;
    int h;
    scanf("%d", &r);
    scanf("%d",&h);
    printf("the volume of the cylinder is %f", pi*r*r*h);
    
}